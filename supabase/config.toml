project_id = "trsccroicslqgsppbjzp"

# Public webhook — called by the telephony provider (Twilio) without auth.
# Authenticated by lookup of the called number in phone_numbers.
[functions.telephony-incoming-call]
verify_jwt = false

# Public WS endpoint — called by the telephony provider.
# Authenticated by call_sid lookup + agent ownership scoping in code.
[functions.voice-stream]
verify_jwt = false

# Public health probe — no PII returned, only status flags.
[functions.elevenlabs-token-check]
verify_jwt = false

# Public webhook — Twilio call status callbacks. Validated via signature.
[functions.telephony-status-callback]
verify_jwt = false
